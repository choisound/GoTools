package main

func main() {

	// up := make([]dao.UserProfile, 0)
	// dao.Query(&up, "select id, acc_name, chinese_name, scholar_field, introduction from user_profile where id >= ? and id <= ?", 1, 20)
	// fmt.Printf("nnnn %+v\n", up)
	// TestSlice()
	// var ups UserProfile
	// ParseInterface(&ups)
}
